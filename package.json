{
  "name": "puppeteer-spammer",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "postinstall": "concurrently 'pnpm --filter ./server install' 'pnpm --filter ./client install'",
    "dev": "concurrently 'pnpm --filter ./server dev' 'pnpm --filter ./client dev'",
    "build": "concurrently 'pnpm --filter ./server build' 'pnpm --filter ./client build' && node scripts/copy-client.js",
    "start": "pnpm --filter ./server start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1",
    "fs-extra": "^11.2.0",
    "pnpm": "^10.15.1"
  }
}
